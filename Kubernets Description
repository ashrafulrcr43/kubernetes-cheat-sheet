Kubernetes Memory Cards

1️⃣ Kubernetes কেন প্রাধান্য পায়:

স্বয়ংক্রিয় ডেপ্লয়মেন্ট, স্কেলিং, ম্যানেজমেন্ট

ম্যানুয়াল সার্ভার কনফিগ কমায়

2️⃣ ReplicaSet:

নিশ্চিত করে নির্দিষ্ট সংখ্যক পড সবসময় চলছে

Deployment ভিতরে ReplicaSet ব্যবহার করে

3️⃣ Service:

পডের একটি সেটকে স্থিতিশীল নেটওয়ার্ক এন্ডপয়েন্ট হিসেবে প্রকাশ করে

ClusterIP, NodePort, LoadBalancer টাইপ

4️⃣ Deployment:

Pod ও ReplicaSet-এর রোলআউট, আপডেট, স্কেলিং নিয়ন্ত্রণ করে

রোলব্যাকও সহজ

5️⃣ Horizontal Pod Autoscaler (HPA):

CPU/মেমোরি বা কাস্টম মেট্রিক অনুযায়ী পড সংখ্যা স্বয়ংক্রিয়ভাবে স্কেল করে

6️⃣ Helm install:

Helm চার্ট ইনস্টল করার কমান্ড: helm install <release-name> <chart-name>

7️⃣ Ingress:

এক্সটারনাল HTTP/HTTPS ট্রাফিক ক্লাস্টারে রাউট করে

TLS, পাথ/হোস্ট ভিত্তিক রাউটিং, লোড ব্যালান্সিং করে

8️⃣ Service টাইপ নয়:

StaticIP → এটি Kubernetes Service টাইপ নয়

Service টাইপ: ClusterIP, NodePort, LoadBalancer

9️⃣ ConfigMap:

সাধারণ কনফিগ ডেটা সংরক্ষণ করে

পডে পরিবেশ ভেরিয়েবল, কমান্ড-লাইন আর্গুমেন্ট বা কনফিগ ফাইল হিসেবে ব্যবহার

🔟 Secret:

সংবেদনশীল ডেটা (পাসওয়ার্ড, টোকেন, কী) এনক্রিপ্টেড বা base64-এ সংরক্ষণ করে

ConfigMap-এর মতো কিন্তু নিরাপদ
